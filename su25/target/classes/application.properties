spring.application.name=su25

# --- MySQL connection ---
spring.datasource.url=jdbc:mysql://localhost:3306/wap?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=MQHmqh2509@
spring.profiles.active=default

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.hikari.maximum-pool-size=5

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# --- Logging ---
logging.level.org.hibernate.SQL=debug
logging.level.org.hibernate.type.descriptor.sql=trace

# Chat Configuration
app.chat.websocket.enabled=true
app.chat.max-message-length=1000
app.chat.typing-timeout=3000

# WebSocket Configuration
spring.websocket.allowed-origins=*

# File Upload for Chat (if needed)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB